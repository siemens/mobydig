/*
Package verifier implements an IP address verifier with caching in order to
avoid expensive duplicate IP address verification.

The concrete IP address verification is then carried out by a Pinger.
*/
package verifier
